// Emil Hedemalm
// 2013-07-28

// Not really applicable,.. just subclass AI directly in your game instead!

/*
#ifndef AI_STATE_MACHINE_H
#define AI_STATE_MACHINE_H

// A default AI State Machine that can be configured either to allocate/deallocate states as it enters them, or just
// call their onEnter/OnExit functions (provided that they have been added to a list of states previously.

class AIStateMachine : public AI {
public:
	
	
private:
	bool allocateOnStateChanges
};

#endif

*/