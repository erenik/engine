/// Emil Hedemalm
/// 2013-11-01
/// Register packet sizes so that they become valid!

#include "SRPacketTypes.h"
#include "Network/NetworkManager.h"

void SRSetPacketSize(){
	assert(false);
	/*
	Packet::SetPacketSize(SRP_PLAYERS, sizeof(SRPPlayers));
	Packet::SetPacketSize(SRP_POSITION, sizeof(SRPPosition));
	Packet::SetPacketSize(SRP_TRANSFORM, sizeof(SRPTransform));
//	NetworkMan.SetPacketSize(SRP_PLAYERS, sizeof(SRPPlayers));
	*/
};