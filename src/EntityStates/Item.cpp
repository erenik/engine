#include "Item.h"

/*
Item::Item(int i_type){
	price = 1;
	strcpy(name, "");
	type = i_type;
};

Food::Food(const char * i_name, int i_price, float i_hungerReplenishedPerSec)
: Item(ItemType::FOOD){
	price = i_price;
	hungerReplenishedPerSecond = i_hungerReplenishedPerSec;
	strcpy(name, i_name);
};

Drink::Drink(const char * i_name, int i_price, float i_thirstReplenishedPerSec)
: Item(ItemType::DRINK){
	price = i_price;
	thirstReplenishedPerSecond = i_thirstReplenishedPerSec;
	strcpy(name, i_name);
}

Tool::Tool(const char * i_name, int i_price, float i_workBonus): Item(ItemType::TOOL){
	price = i_price;
	workBonus = i_workBonus;
	strcpy(name, i_name);
};

Vehicle::Vehicle(const char * i_name, int i_price, float i_movementSpeedBonus): Item(ItemType::VEHICLE){
	price = i_price;
	movementSpeedBonus = i_movementSpeedBonus;
	strcpy(name, i_name);
};
*/